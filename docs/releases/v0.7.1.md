## v0.7.1

今回の更新では、初回起動フローとアプリ内 UI を見直しました。

### 変更点

- 初回起動時だけセットアップ画面を表示するように修正
- 2 回目以降は起動直後にメイン画面へ入るように改善
- アップデート後に古い runtime が残っていても、自動で新しい runtime を反映するように修正
- ネイティブ app のセットアップ画面を再設計
- 設定ウィンドウを見やすい 2 カラム構成に整理
- ブラウザ版 / アプリ内 Web UI の hero と上部導線を圧縮し、作業エリアを上に寄せるよう改善
- スクリーンショットを最新 UI に差し替え

### 確認したこと

- クリーン状態での初回起動スクリーン確認
- 既存 runtime がある状態での起動確認
- 設定ウィンドウの実表示確認
- `python3 -m py_compile app.py`
- `swiftc -typecheck`
- `build_launcher_app.sh`, `build_zip.sh`, `build_dmg.sh`
